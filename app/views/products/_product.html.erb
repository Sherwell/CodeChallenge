<%= link_to product_open_modal_path(product), remote: true do %>
	<div class="col-sm-4 prod-grid-item">
		<p><%= product.name %></p>
		<p><%= product.brand %></p>
		<p><%= product.model %></p>
		<p>$<%= product.price %></p>
	</div>
<% end %>